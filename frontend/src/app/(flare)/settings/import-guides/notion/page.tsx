import { ImportGuidePage } from "@/features/settings/import-guide-page";

export default function NotionImportGuide() {
  return (
    <ImportGuidePage
      source="Notion"
      preparation="Use Notion's official export tools to produce content you can select as Markdown, text, or CSV."
    />
  );
}

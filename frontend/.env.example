NEXT_PUBLIC_API_URL=/api
# Use "mock" for the standalone browser demo.
NEXT_PUBLIC_DATA_PROVIDER=api

# Must be set at both build and runtime when the backend address changes.
API_INTERNAL_URL=http://127.0.0.1:8000
